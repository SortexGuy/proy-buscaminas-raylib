#include <iostream>
#include <thread>
#include "raylib.h"

int main(int argc, char* argv[]) {
  InitWindow(960, 540, "Buscaminas Estructurado");
  SetTargetFPS(60);
  WaitTime(2);
  CloseWindow();
  return 0;
}
